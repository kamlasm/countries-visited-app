<nav>
    <% if (user) { %>
        <p>Hello, <%= user.username %></p>
        <div class="nav-links">
            <a href="/user/<%= user.userId %>">My Countries</a>
            <a href="/community"> Community</a>
            <a href="/user/<%= user.userId %>/settings">Settings</a>
            <a href="/auth/sign-out"> Sign Out</a>
        </div>
    <% } else { %>
        <p class="nav-links"><a href="/">Home</a></p>
    <% } %>
</nav>