<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit a country</title>
</head>
<body>
    <%- include('../partials/nav.ejs') %>
    <h1>Edit details!</h1>
    <form action="/user/<%= user.userId %>/<%= userCountry._id %>/edit?_method=PUT" method="POST">
        <label for="countryName">Country: </label>
        <select name="countryName" disabled>
            <% country.forEach(country => { %>
                <% if (country.name === userCountry.countryName.name) { %>
                    <option value="<%= country.name %>" selected="selected"><%= country.name %></option>
                <% } else { %>
                    <option value="<%= country.name %>"><%= country.name %></option>
                <% } %>
            <% }) %>
        </select>
        <p>Edit details about the visit (optional)</p>
        <div>
            <label for="startDate">Start Date: </label>
            <input type="date" name="startDate" value="<%= startDate %>">
        </div>
        <div>
            <label for="endDate">End Date: </label>
            <input type="date" name="endDate" value="<%= endDate %>">
        </div>
        <div>
            <label for="highlights">Highlights: </label>
            <textarea type="text" name="highlights"><%= userCountry.highlights %></textarea>
        </div>
        <div>
            <label for="travellerTips">Traveller Tips: </label>
            <textarea type="text" name="travellerTips"><%= userCountry.travellerTips %></textarea>
        </div>
        <div>
            <label for="images">Photos: </label>
            <input type="file" name="images">
        </div>       
        <button type="submit">Edit visit details</button>
        <a href="/user/<%= user.userId %>/<%= userCountry._id %>">Back to <%= userCountry.countryName.name %></a>
    </form>
</body>
</html>